@model int;

@if (Model > 0)
{
    <li class="nav-item">
        <a class="nav-link text-light" style="font-size: 20px;" asp-controller="Orders" asp-action="ShoppingCart">
            <i class="bi bi-cart4"></i> @Model
        </a>
    </li>
}